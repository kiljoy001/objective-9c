class Counter {
    int64 val;
    method Counter(int64 n) { val = n; }
    method int64 getValue() { return val; }
    method void inc(int64 n) { val = val + n; }
}

func main() {
    Counter c = new Counter(10);
    c.inc(5);
    while(c.getValue() < 1000) {
        c.inc(1);
    }
    print("done");
}
