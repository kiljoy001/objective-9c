class Counter {
    int64 val;
    method Counter(int64 n) { val = n; }
    method int64 get() { return val; }
    method void inc(int64 n) { val = val + n; }
}

main {
    Counter c = new Counter(100);
    print("grid counter up\n");
    string wait = readline();
}
