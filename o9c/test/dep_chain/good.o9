function chain() int64 {
    use { chain }

    int64 n;
    c {
        n = O9_BASE_VALUE + O9_CHAIN_VALUE;
    }
    return n;
}

main {
    Task<int64> t = spawn chain();
    print("chain ", t.await(), "\n");
}
