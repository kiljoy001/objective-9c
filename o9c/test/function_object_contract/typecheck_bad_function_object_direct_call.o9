class Runner {
    prop function bad;

    method Runner() {
        bad = function reach() int64 {
            return 1;
        };
    }

    method int64 call() {
        return bad.run();
    }
}

main {
    Runner r = new Runner();
    print(r.call(), "\n");
}
