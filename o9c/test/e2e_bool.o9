class BoolBox {
    prop bool a;
    prop bool b;

    method BoolBox(bool x, bool y) {
        a = x;
        b = y;
    }

    method bool both() {
        return a && b;
    }

    method bool either() {
        return a || b;
    }

    method bool flipped() {
        return !b;
    }

    method bool compare(int64 x, int64 y) {
        return x < y;
    }
}

class BoolDefaults {
    prop bool flag;

    method bool value() {
        return flag;
    }
}

main {
    BoolBox t = new BoolBox(true, false);
    BoolDefaults d = new BoolDefaults();
    bool local;
    print("both ", t.both(), "\n");
    print("either ", t.either(), "\n");
    print("flipped ", t.flipped(), "\n");
    print("compare ", t.compare(3, 9), "\n");
    print("default object ", d.value(), "\n");
    print("default local ", local, "\n");
}
