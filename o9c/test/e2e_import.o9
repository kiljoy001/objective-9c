// Multi-file o9: selective import pulls Greeter from lib.o9 in the same
// dir. Resolved relative to THIS file; may only reach this subtree.
from "impdir/lib.o9" import Greeter;
func main() {
    Greeter g = new Greeter();
    g.setbase(100);
    print("answer ", g.answer(), "\n");   // 100 + 42 = 142
}
