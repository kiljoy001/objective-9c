class LogicTest {
    int64 val;
    string s;
    method void run() {
        bool b = true;
        bool b2 = false;
        if (b && !b2) {
            val = (1 << 5) | 0x01;
            s = "hello logic";
        }
        val = -val;
        val = ~val;
    }
}
