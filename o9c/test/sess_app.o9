// App for the session-isolation test: two Counter instances that two
// concurrent sessions drive independently.
class Counter {
    prop int64 val;
    method void set(int64 n) { val = n; }
    method int64 get() { return val; }
}
main {
    Counter a = new Counter();
    Counter b = new Counter();
    serve();
}
